#include <iostream>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n, m;
        cin >> n >> m;
        int c[m], d[n], f[n], b[n];
        long long int profit=0;
        int r[n];
        for(int i=0; i<m; i++)
            cin >> c[i];
        for(int i=0; i<n; i++)
            cin >> d[i] >> f[i] >> b[i];
        for(int i=0; ; )
        {

        }
    }
    return 0;
}
