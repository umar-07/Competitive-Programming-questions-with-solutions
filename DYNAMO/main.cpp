#include <iostream>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        cin.sync();
        long long int n;
        long long int umar;
        long long int one;

        long long int doo;
        long long int eel;
        long long int sum=1;
        long long int boo;
        long long int chutiya;
        cin >> n >> umar;
        long long int poo=n;
        while(poo--)
        {
            sum=sum*10;
        }
        long long int news=sum;
        sum*=2;
        sum+=umar;

        long long int ak47=sum;
        cout << sum << endl;
        cin >> boo;

        ak47+=boo;
        cout << (news-boo) << endl;

        cin >> doo;

        ak47+=doo;

        cout << (news-doo) << endl;

        cin >> one;

        if(one!=1)
            break;
    }
    return 0;
}
