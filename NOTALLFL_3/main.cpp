#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        ll n, k;
        cin >> n >> k;
        ll arr[n], h[k+1]={0};
        for(int i=0; i<n; i++)
            cin >> arr[i];
        ll f=0;
        ll maxi=0;
        for(int i=0; i<n; i++)
        {

        }
    }
	return 0;
}
